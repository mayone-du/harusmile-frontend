query GetPlan($planId: ID!) {
  plan(id: $planId) {
    id
    title
    content
    price
    planImage
    isPublished
    createdAt
    publishedAt
    planAuthor {
      id
      targetUser {
        profileName
        profileImage
      }
    }
  }
}
